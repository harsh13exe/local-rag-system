# Python
venv/
__pycache__/
*.pyc
*.pyo
.Python

# Vector store (regenerated by ingest.py, don't commit binary DB)
chroma_db/

# Environment
.env

# Eval results (optional — keep if you want to show it, remove this line if so)
eval_results.json

# OS
.DS_Store

# Sensitive documents — remove this line if you WANT your resume public in the repo
documents/
